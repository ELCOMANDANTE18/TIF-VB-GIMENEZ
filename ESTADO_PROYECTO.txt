LINK SEGURO — Estado del proyecto
===================================
Fecha: 2026-06-03
Autor: Victor Benjamin Gimenez
Materia: TIF-3 / Universidad de Mendoza


VERSIÓN ACTUAL: v0.4.0
-----------------------
El sistema está operativo en modo desarrollo local con túnel ngrok.
Pipeline completo: detección heurística + respuesta automática por
Instagram DM + notificación por email al dueño de la cuenta.


LO QUE ESTÁ CONSTRUIDO
-----------------------
[v0.1] Integración con Meta Webhook + persistencia en Supabase.
[v0.2] Migración a SQLite local. Pipeline de análisis completo:
       URL Analyzer + Text Analyzer + IA UM Cloud (gemma4-26b).
       Prompt ajustado a norma académica. Fix de id_conversacion
       bidireccional (sha256 del par sender/recipient).
[v0.3] Dashboard web con login multiusuario (bcrypt + cookies firmadas).
       Auth modular (auth.py). Módulo de notificaciones (messenger.py).
       Scripts de setup de usuarios y migración de IDs.
       Documentación técnica completa en src/docs/.
[v0.3.1] Fix respuesta automática end-to-end:
       - orchestrator usa FLIA_TEST_TOKEN (token de fliagimenez2026).
       - Fix NoneType en groq_client cuando el modelo devuelve content=None.
       - Limpieza: eliminados stubs muertos app/utils.py y app/models.py.
       FLUJO VERIFICADO: gimenezbenja2 → DM a fliagimenez2026 →
       webhook → análisis → fliagimenez2026 responde a gimenezbenja2 ✅
[v0.4] Notificaciones por email (2026-06-03):
       - app/notifications/email_notifier.py (smtplib stdlib, STARTTLS).
       - Email HTML con badge de riesgo, datos MITRE, recomendaciones.
       - Columna email agregada a usuario_sistema (migración automática).
       - Orchestrator conectado: dispara email en HIGH risk.
       - SMTP configurado con Gmail app password (linkseguro.tif@gmail.com).
       - scripts/reset_analisis.py para limpiar BD entre pruebas.
       VERIFICADO: email de alerta llega correctamente ✅
       PENDIENTE: actualizar emails reales de cada usuario del dashboard.
       PENDIENTE: login con email O username (próxima iteración).


ARQUITECTURA ACTUAL
--------------------
  gimenezbenja2 envía DM sospechoso
          │
          ▼
  fliagimenez2026 (cuenta monitoreada, token: FLIA_TEST_TOKEN)
          │
          ▼
  Meta Webhook → POST /webhook (ngrok → localhost:8000)
                       │
                 BackgroundTask
                       │
          PhishingOrchestrator.analyze()
            ├── URLAnalyzer  (PhishTank 29k + URLhaus + Regex)
            ├── TextAnalyzer (patrones de ingeniería social)
            ├── URLhaus API  (confirmación online async)
            └── UM Cloud AI  (gemma4-26b — CAÍDO al 2026-06-03)
                       │
                  SQLite (aiosqlite)
                       │
          ┌────────────┼────────────────┐
          ▼            ▼                ▼
  Dashboard /dashboard  DM automático   Email HTML
  (login multiusuario)  al remitente    a dueño de cuenta
                        (Instagram)     (Gmail SMTP) ✅


ESTADO DE COMPONENTES (2026-06-03)
------------------------------------
  ✅ Webhook Meta                — operativo
  ✅ URLAnalyzer + TextAnalyzer  — operativo (PhishTank 29k dominios)
  ✅ SQLite + aiosqlite          — operativo
  ✅ Dashboard web               — operativo (login multiusuario)
  ✅ Respuesta automática DM     — verificada end-to-end
  ✅ Notificación por email      — verificada (linkseguro.tif@gmail.com)
  ❌ UM Cloud AI (gemma4-26b)    — caído (HTTP 400 en todos los modelos)
                                   Sin IA el score topa en ~0.50 (MEDIUM).
                                   Pendiente reporte al equipo UM.


TOKENS Y CUENTAS (.env)
------------------------
  PAGE_ACCESS_TOKEN     → gimenezbenja2  (cuenta del desarrollador)
  FLIA_TEST_TOKEN       → fliagimenez2026 (cuenta monitoreada) ← usa el sistema
  FLIA_TEST_IG_USER_ID  → 26628591963436168
  SMTP_USER             → linkseguro.tif@gmail.com ← cuenta de alertas
  ENABLE_EMAIL_ALERTS   → true

  NOTA: Los tokens de Instagram duran ~60 días.
  Renovar en developers.facebook.com → Graph API Explorer.


USUARIOS DEL DASHBOARD
-----------------------
  username   password   ig_username        email
  ─────────────────────────────────────────────────────────────────────
  admin      admin123   —                  linkseguro.tif@gmail.com   ✅ (= SMTP_USER)
  flia_test  link2024   fliagimenez2026    fliagimenez2026@gmail.com  ✅
  hernesto   link2024   hernestolopez2026  hernestolopez2026@gmail.com ✅
  benja      link2024   gimenezbenja2      gimenezbenja2@gmail.com    ✅

  Login acepta username O email (auth.py + formulario actualizado).    ✅


PRÓXIMOS PASOS
--------------
[x] Actualizar emails reales de los usuarios en setup_usuarios.py y BD.

[x] Login con email O username:
    - auth.py busca por username OR email.
    - Formulario de login: label "Usuario o email" + placeholder.

[ ] Esperar que UM Cloud corrija su servidor (HTTP 400 en /chat/completions).
    Cuando vuelva, la integración de IA funciona sin cambios en el código.

[ ] Activar verificación HMAC de firma Meta en el webhook
    (validator.py ya implementado, falta wirearlo en router.py).

[ ] Evaluar el pipeline con el dataset real:
    tests/dataset_evaluacion.json (8 casos etiquetados).
    Registrar precision/recall para el informe TIF.

[ ] Redactar informe académico final apoyándose en:
    src/docs/DECISIONES_ARQUITECTURA.md
    src/docs/PROMPT_SUSTENTO.md
    src/docs/BASE_DE_DATOS.md

[ ] Renovar tokens Instagram antes de la presentación (~60 días desde emisión).


NOTAS
-----
- Supabase descartado (decisión tutor 2026-05-08). Solo SQLite.
- CLAUDE.md y AGENTS.md ignorados por .gitignore (no van al repo).
- Cambiar SESSION_SECRET antes de cualquier despliegue real.
- app/utils.py y app/models.py eliminados (stubs muertos, 2026-06-03).
- Para limpiar análisis entre pruebas: python scripts/reset_analisis.py
