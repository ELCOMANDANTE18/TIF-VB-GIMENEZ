═══════════════════════════════════════════════════════════════════
  LINK SEGURO — PARA RETOMAR
  Última actualización: 2026-05-29 (final de sesión)
  Complementa a ESTADO_SESION.txt — acá quedan los pendientes y
  decisiones de la última vuelta.
═══════════════════════════════════════════════════════════════════


LO QUE PASÓ AL FINAL DE LA SESIÓN
───────────────────────────────────────────────────────────────────

1. Re-import de gimenezbenja2
   - El export inicial trajo 5 JSONs; un re-export trajo 12 JSONs
     completos. Re-corrimos `import_conversations.py`.
   - Resultado: 2253 mensajes importados (idempotente, sin duplicar).
   - Dashboard de benja pasó de 4 conv / 71 msg → 9 conv / 978 msg.

2. Fix del bug id_conversacion (no bidireccional)
   - Archivo: app/db/sqlite_client.py — función save_message.
   - Antes:  id_conversacion = sha256(sender_id + recipient_id)
   - Ahora:  a, b = sorted([sender_id, recipient_id])
             id_conversacion = sha256(a + b)
   - Verificado: el mismo hash para entrante y saliente entre
     dos cuentas dadas (mismas dos partes → mismo conv_id).
   - uvicorn corre con --reload, ya tomó el cambio.


LO QUE EL FIX ARREGLA
───────────────────────────────────────────────────────────────────

   ✅  Mensajes NUEVOS que lleguen vía webhook unifican entrante
       y saliente en una sola row de `conversacion`.

   ❌  NO toca los rows ya guardados. Las históricas (importadas
       de Graph API + las viejas de flia_test) siguen partidas.


CONVERSACIONES "RARAS" EN LA BD
───────────────────────────────────────────────────────────────────

  Algunas personas aparecen con MENOS mensajes de los esperados en
  el dashboard de benja, porque buena parte de la conversación
  quedó en otra row con cuenta_monitoreada = ID de la otra persona.

  Ejemplos visibles para benja:
    - lautaro._.1908  → 609 msg visibles (en JSON: 3589 total)
    - axenide         → 172 msg visibles (en JSON: 1430 total)
    - federicodavara  → 67 msg visibles  (en JSON: 168 total)
    - hernestolopez   → 2 msg visibles   (entrantes del webhook +
                                          1 entrante del import)

  El resto de mensajes existe en la BD pero con `cuenta_monitoreada`
  igual al ID de la otra persona (los SALIENTES de gimenezbenja2).

  Razón técnica: en import_conversations.py, cuando el mensaje es
  saliente, `recipient_id = external_id` y por eso
  `cuenta_monitoreada = external_id` (no benja). El primer INSERT
  define ambos campos y el ON CONFLICT no los actualiza.


PENDIENTE PRINCIPAL — MIGRACIÓN DE HISTÓRICAS
───────────────────────────────────────────────────────────────────

  Para que benja vea TODOS los mensajes (entrantes + salientes) en
  una sola row por persona, hay que correr una migración SQL que:

    1. Recalcule id_conversacion para todos los rows existentes
       usando el hash bidireccional.
    2. Fusione rows que ahora colisionan (mismos dos IDs).
       Sumar total_mensajes, tomar el max(ultimo_mensaje_at),
       mantener cuenta_monitoreada = la del usuario monitoreado
       (no del externo).
    3. Hacer lo mismo en tabla `mensaje` (su id_conversacion FK).

  Alternativa más simple (recomendada si la fidelidad histórica
  no es crítica para la demo):

    a) Borrar todos los rows con
       cuenta_monitoreada NOT IN (lista de ig_user_id de usuarios
       no admin, p.ej. 17841407634191670 para benja).
    b) Re-importar los JSONs forzando que recipient_id sea
       SIEMPRE el ID monitoreado (modificar import_conversations.py
       para que pase app_scoped_id como recipient_id incluso en
       salientes, y el `es_entrante` se calcule por separado).

  Decidir antes de la demo si vale la pena el esfuerzo o no.


OTROS PENDIENTES (sin orden)
───────────────────────────────────────────────────────────────────

  [ ] Las históricas importadas NO se analizaron por el orchestrator
      (siguen con risk_level=LOW por default). Armar
      `scripts/analyze_imported.py` si querés que aparezcan con
      score real y categoría MITRE.

  [ ] SESSION_SECRET en .env sigue siendo "link_seguro_secret_2024".
      Cambiar por algo random antes de mostrar el sistema a
      terceros (`python -c "import secrets; print(secrets.token_urlsafe(48))"`).

  [ ] hernestolopez2026 sigue sin DMs en su filtro:
      - Su token (Ernesto_TEST_TOKEN) está vacío en .env.
      - Su IGSID de webhook NO se verificó (en setup_usuarios.py
        figura 955769153705305, que es lo que pediste en el prompt,
        pero podría no ser el ID app-scoped del webhook).
      - Para validarlo: pedirle que se loguee en su cuenta IG,
        autorizar la app, y mandar un DM a benja. El payload va
        a traer el ID que Meta le asigna a hernesto.

  [ ] Considerar agregar /users en el dashboard para que admin
      pueda crear/editar usuarios sin tocar setup_usuarios.py.


COMANDOS ÚTILES PARA RETOMAR
───────────────────────────────────────────────────────────────────

  # Arrancar todo
  cd src
  .venv/bin/uvicorn app.main:app --reload --port 8000
  ngrok http 8000        # otra terminal — copiar URL y pegar en Meta

  # Inspeccionar la BD
  sqlite3 data/phishing_detector.db
  > .schema usuario_sistema
  > SELECT cuenta_monitoreada, COUNT(*), SUM(total_mensajes)
    FROM conversacion GROUP BY cuenta_monitoreada;

  # Re-import (idempotente)
  .venv/bin/python scripts/import_conversations.py \
    --dir data/conversations/gimenezbenja2

  # Re-export desde Graph API
  TOKEN=$(grep "^Personal_TEST_TOKEN=" .env | cut -d= -f2-)
  .venv/bin/python export_conversations.py \
    --token "$TOKEN" \
    --ig-user-id 26555696997406448 \
    --app-scoped-id 17841407634191670 \
    --output-dir data/conversations/gimenezbenja2


ESTADO ACTUAL DE LA BD (al cierre de sesión)
───────────────────────────────────────────────────────────────────

  Por cuenta_monitoreada:
    17841407634191670 (benja)         →  9 conv,  978 msg
    17841428323852479 (flia_test old) →  8 conv,   75 msg
    955769153705305   (hernesto)      →  0 conv,    0 msg
    + otros IDs (salientes históricos repartidos)

  Total: 2440+ mensajes en SQLite, 16+ conversaciones.

  Usuarios para probar vistas (sin cambios desde ESTADO_SESION.txt):
    admin     / admin123   → ADMIN, ve todo
    flia_test / link2024   → 8 conv (históricas viejas)
    hernesto  / link2024   → 0 conv
    benja     / link2024   → 9 conv (gimenezbenja2 — tu personal)
