RETOMAR — Link Seguro
======================
Última sesión: 2026-06-03


ESTADO AL CERRAR
-----------------
Todo funciona. Servidor apagado. Hacer commit antes de arrancar.

Archivos modificados pendientes de commit (sesión 2026-06-03):
  - src/scripts/setup_usuarios.py              (emails reales + admin = SMTP_USER)
  - src/app/dashboard/auth.py                  (login por username OR email)
  - src/app/dashboard/router.py                (filtros admin + botones acción + endpoints)
  - src/app/notifications/email_notifier.py    (template bienvenida + send_welcome_email)
  - src/scripts/test_email.py                  (3 escenarios: bienvenida + HIGH + MEDIUM)
  - ESTADO_PROYECTO.txt                        (actualizado)
  - PROXIMOS_PASOS.txt                         (nuevo — roadmap de funcionalidades)


PARA ARRANCAR
--------------
cd ~/Escritorio/TIF-3/TIF-VB-GIMENEZ/src
source .venv/bin/activate
uvicorn app.main:app --reload --port 8000
ngrok http 8000   ← en otra terminal

Dashboard: http://localhost:8000/dashboard  (admin / admin123)
Inspector: http://127.0.0.1:4040


LO QUE SE AGREGÓ EN ESTA SESIÓN
---------------------------------
1. Emails reales en BD:
     flia_test  → fliagimenez2026@gmail.com
     hernesto   → hernestolopez2026@gmail.com
     benja      → gimenezbenja2@gmail.com
     admin      → linkseguro.tif@gmail.com  (= SMTP_USER del .env)

2. Login acepta username O email (auth.py + formulario actualizado).

3. Email de bienvenida: send_welcome_email() en email_notifier.py.
   Template HTML verde con explicación del sistema y botón al dashboard.

4. Test de emails: scripts/test_email.py
   3 escenarios × 4 usuarios = 12 emails (bienvenida + HIGH + MEDIUM).

5. Filtros por cuenta en dashboard admin (barra de botones + ?filtro= en URL).
   Los contadores de riesgo también se filtran.

6. Botones de acción manual en cada conversación (▼ Ver → sección Acciones):
     💬 Responder DM   — dispara Instagram Graph API al remitente.
                         Solo funciona si la cuenta tiene token en .env.
                         Actualmente: solo fliagimenez2026 (FLIA_TEST_TOKEN).
     📧 Notificar usuario — admin manda email de alerta al dueño de la cuenta.
   Ambos usan fetch() (sin recarga). Feedback visual inmediato.


PENDIENTES INMEDIATOS
----------------------
1. COMMIT
   git add .
   git commit -m "feat: v0.4.1 — bienvenida email + filtros admin + botones acción manual"

2. VER PROXIMOS_PASOS.txt para decidir qué construir después.
   Candidatos principales:
     [A] Evaluación dataset (precision/recall) — para el informe TIF
     [B] Página de estadísticas en el dashboard
     [C] Marcar conversación como falso positivo / confirmado
     [E] Exportar análisis a CSV


CONTEXTO TÉCNICO IMPORTANTE
-----------------------------
TOKENS:
  PAGE_ACCESS_TOKEN    → gimenezbenja2      (NO es la cuenta monitoreada)
  FLIA_TEST_TOKEN      → fliagimenez2026    (cuenta monitoreada) ← usa el sistema
  FLIA_TEST_IG_USER_ID → 26628591963436168
  SMTP_USER            → linkseguro.tif@gmail.com (verificado, funciona)
  ENABLE_EMAIL_ALERTS  → true
  Tokens Instagram expiran ~60 días → renovar antes de la presentación.

FLUJO VERIFICADO:
  gimenezbenja2 → DM a fliagimenez2026
  → webhook → análisis → DM automático ✅ → Email HTML ✅

USUARIOS DASHBOARD:
  admin     / admin123  → ADMIN, ve todo + filtros + botón Notificar
  flia_test / link2024  → fliagimenez2026         (solo sus conversaciones)
  hernesto  / link2024  → hernestolopez2026        (solo sus conversaciones)
  benja     / link2024  → gimenezbenja2            (solo sus conversaciones)

RESET DE BD ENTRE PRUEBAS:
  python scripts/reset_analisis.py

BD ACTUAL (2026-06-03):
  20 conversaciones | 2340 mensajes | 4 análisis registrados
  (conversaciones importadas de histórico, análisis solo de pruebas en vivo)
